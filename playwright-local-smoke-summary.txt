Playwright @smoke local run — summary for planning fixes
========================================================
Run: npx playwright test --grep "@smoke" (headless default), ~10.6 min, 5 workers
Result: 86 failed, 101 skipped, 146 passed

Artifacts
---------
- Full console log (ANSI):  playwright-local-smoke.log  (repo root)
- HTML report:            web/playwright-report/index.html  (open in browser)
- Per-test output:         web/test-results/  (screenshots, videos, error-context.md)

Global setup
------------
- Log: "[global-setup] Signup failed — saving empty auth state"
- Effect: authedPage-based tests get empty auth; many account/nav flows fail downstream.

Failure themes (group for fixes)
----------------------------------

1) Account + auth UI (chromium + mobile, ~10 tests)
   - account.spec.ts: login redirect, free/pro plan copy, nav avatar → /account
   - signup-login.spec.ts: validation messages (30s timeouts → likely wrong page / blocked UI)
   Likely: missing real auth backend + empty global-setup state; may need mocks or ?tier= flows.

2) Analytics (chromium + mobile, 4 tests)
   - analytics/navigation.spec.ts: simulator tab bar, profiles tab, landing links
   Likely: route/feature flag, or auth-gated analytics.

3) FairBet / odds / CLV / history (chromium + mobile, many tests)
   - fairbet.spec.ts: tier badges, chips, line blur, CLV modals (~31s timeouts)
   - fairbet/odds.spec.ts: "How it works" explainer
   - fairbet/source-attribution.spec.ts: bet cards
   - fairbet/phase9.spec.ts: /history gate + pro content (still failing locally)
   Likely mix: (a) no bet cards / slow API, (b) ?tier= / E2E flag parity on client for history,
   (c) parallel load on single webServer.

4) Freemium — ProGateSheet + tier store + tier-gating (chromium + mobile, ~18 tests)
   - pro-gate-sheet.spec.ts, tier-gating-suite.spec.ts: "__openProGateSheet not mounted"
   - pro-gate.spec.ts: getStoredTier null, cookies not set (very fast failures ~1s)
   Likely: home page not finishing hydration before evaluate; or client bundle without
   NEXT_PUBLIC_SCROLLDOWN_E2E when dev server started without that env (verify webServer.env).

5) Billing API (chromium + mobile, 2–3 tests)
   - billing.spec.ts portal 404: signInAs → sd-session missing from request.storageState()
     after magic-link verify redirect (Playwright APIRequestContext cookie jar issue).
   - billing ProGateSheet tests: __openProGateSheet timeout 30s (same as (4)).

6) Story generation (chromium + mobile, 10 tests)
   - story-generation.spec.ts: NBA copy tests + API contract (400/503)
   Likely: ANTHROPIC_API_KEY / story API unavailable — align with skip-if-unavailable pattern.

7) PWA / Service worker (chromium + mobile, ~12 tests)
   - install-and-offline, offline-scenarios, service-worker specs
   Likely: SW registration on localhost HTTP, timing, or route interception flakiness under load.

Suggested next steps
----------------------
1. Open web/playwright-report/index.html and sample web/test-results/*/error-context.md for screenshots.
2. Fix global-setup signup or stub authedPage for CI/local without live auth.
3. Unify "wait for ProGateSheet hook" across pro-gate-sheet + tier-gating-suite (same helper as billing).
4. Confirm NEXT_PUBLIC_SCROLLDOWN_E2E reaches client on `npm run dev` (playwright webServer) for tier/history.
5. Fix signInAs to capture session cookie (storageState after browser context, or cookie header from verify).
6. Story/PWA: explicit test.skip when deps missing or relax timeouts under parallel workers.
